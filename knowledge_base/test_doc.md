# Python 函数学习笔记

Python 函数用于封装一段可以重复执行的代码。

函数可以接收参数，包括位置参数、关键字参数、默认参数和可变参数。

在人工智能项目中，函数常用于封装数据预处理、模型训练、模型预测和结果评估流程。

# RAG 检索增强生成

RAG 的全称是 Retrieval-Augmented Generation，也就是检索增强生成。

它的核心流程是：先根据用户问题从知识库中检索相关资料，然后把资料作为上下文交给大语言模型生成答案。

RAG 可以减少大模型胡编乱造，提高回答的准确性和可追溯性。
