# Progress

## 2026-09-11 — Recovery fix checkpoint

- `deploy/learning_containers.py` was untracked along with the broader release tooling. Commit its standalone observation implementation and tests so the DNS helper is usable without pulling in unfinished release orchestration.
- Compatibility proves the original digest by varying only null/empty representations of Dns, DnsOptions and DnsSearch. Saved receipts and normal hashes remain unchanged; nonempty DNS and unrelated configuration changes are rejected.
- `venv/Scripts/python.exe -m pytest -q tests/deploy/test_learning_containers.py --basetemp=.pytest-tmp-dns-commit --tb=short`: 38 passed, exit 0.
- Recovery finish was independently verified in the preceding session; its operation-specific driver and receipts stay outside Git. Candidate build will use precisely 5e311e3, while current HEAD also contains later UI/startup fixes.

## 2026-09-11 — Exact candidate validation; release blocked

- **Recovery commit:** `9a5fd74` contains the standalone container observer/DNS compatibility, 38 passing tests, recovery documentation and task records. `compose.release.yaml` is ignored; no operation driver, receipt, credentials, backup or key was included.
- **Source:** clean detached worktree `.worktrees/release-5e311e3`, full revision `5e311e3624eec5335b738aa54c70b68224fcf397`. Later UI/startup changes were excluded.
- **Build:** `docker build --pull=false --label org.opencontainers.image.revision=5e311e3 -t zhiyan:candidate-5e311e3-20260911 --iidfile D:/python_self_agent/.runtime/candidate-5e311e3.iid .` in that worktree succeeded. First attempt failed fetching Docker Hub metadata with EOF; retry succeeded. Inspected image ID: `sha256:00635b5746e3db8b8da4bea0f64a9957aa5578cac993a8ec31c9103c275d337e`; revision label is `5e311e3`.
- **Baseline:** `D:/python_self_agent/venv/Scripts/python.exe -m pytest -q tests/deploy/test_image_contract.py tests/deploy/test_compose_contract.py tests/test_learning_plan_schema.py tests/test_learning_migration.py tests/test_note_source_deletion.py --basetemp=.pytest-tmp-release-base --tb=short` in the exact worktree: **59 passed**.
- **Isolation:** local harness `.runtime/validate-candidate-5e311e3.py` used newly created App, Qdrant and backup volumes, synthetic accounts and loopback ephemeral ports. No production data mounts or credentials were used. All test containers and volumes were removed; independent Docker inventory confirmed cleanup.
- **Passed:** old-image schema seeded with a synthetic user/note; candidate `initialize_database` retained all old table rows and added six empty learning tables; registration, note creation and learning-plan listing succeeded. After restoring paired cold test backups, SQLite table contents matched the pre-upgrade snapshot, the Qdrant test point payload was recovered, and the old image served health successfully.
- **Failed gate:** with `ZHIYAN_MAINTENANCE_MODE=1` and a fresh operation ID, `/healthz` returned **200** (required 503), `/maintenancez` returned **404** (required JSON 200), and registration/note writes still succeeded. `server.py` unconditionally creates the normal application; the image entrypoint launches it without maintenance dispatch. The Dockerfile also does not package the current offline release-upgrade tooling.
- **Limits:** this validates the schema initializer and synthetic paired restore, not the missing production offline-upgrade command. App tests used JSON/simple embeddings and root for fixture volumes; they do not establish production UID permissions or live BGE-M3 inference. The failed maintenance gate already disqualifies this candidate.
- **Production:** App/Qdrant container IDs, image IDs and start timestamps were identical before and after verification. Production was not redeployed. No new release operation was created and the finalized recovery operation was not reused.
- **Evidence:** local ignored `.runtime/candidate-5e311e3-validation.json` reports ten passing checks, one failed gate, no harness/cleanup errors, and `eligible_for_release: false`. Build and test logs remain local under `.runtime/`; runtime evidence is excluded from this documentation commit.
- **Next:** implement and test maintenance startup/write isolation and package the required offline-upgrade entrypoint in a new revision; rebuild and rerun release gates before creating a fresh release operation. The original 5e311e3 candidate remains rejected.
