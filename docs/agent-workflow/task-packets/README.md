# Task packet workspace

Create one subdirectory per reviewed plan. Do not place implementation notes or
unreviewed scratch work directly in this directory.

Each plan directory contains one `REVIEW.md` and numbered task packets. Only
packets with `status: ready` may be assigned to Claude Code.
