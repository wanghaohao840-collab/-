# BGE-M3 Candidate Validation Plan

Goal: rescan a published candidate and persist an exact source/candidate/target validation proof. Build a new target inventory using authoritative source owners; compare every namespace/document/user count, byte count and model-independent content digest; reject unknown/missing scope; verify live source before and after; exclusive-create a safe, fingerprinted result. No quality scoring, registry activation, controller, live data or deletion.

Files: candidate_validation.py, test_candidate_validation.py and workflow/spec/output records. Verify focused candidate/source tests, all Memory/RAG, compile, pip and diff. Serial inline execution already authorized.
