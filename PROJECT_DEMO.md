# 智能文档学习助手项目演示流程

本文档用于记录智能文档学习助手项目的完整演示流程，方便项目复现、展示和讲解。

---

## 1. 启动项目

在 PowerShell 中进入项目目录：

```powershell
cd D:\python_self_agent
```

运行 Gradio 应用：

```powershell
D:\Anaconda\python.exe D:\python_self_agent\ui\gradio_app.py
```

运行成功后，浏览器访问：

```text
http://127.0.0.1:7860
```

---

## 2. 上传文档

进入页面中的 **“1. 上传文档”** 选项卡。

当前支持以下格式：

```text
.pdf
.txt
.md
.markdown
.docx
```

选择一个本地文档文件，然后点击：

```text
导入文档
```

系统会自动完成以下操作：

* 文档文本解析
* 文本切分
* 生成文本向量
* 写入本地 RAG 缓存
* 自动刷新文档下拉框

导入成功后，可以在文档问答和文档检索页面中选择该文档。

---

## 3. 文档问答

进入 **“2. 文档问答”** 选项卡。

操作步骤：

```text
1. 在下拉框中选择当前文档
2. 点击“切换当前文档”
3. 输入问题
4. 点击“开始问答”
```

示例问题：

```text
这个文档主要讲了什么？
RAG 的核心流程是什么？
这个 Word 文档讲了什么？
这个 Markdown 文档有哪些重点？
```

系统会基于当前选中的文档生成回答，并显示参考来源。

如果是 PDF 文档，系统会尽量显示参考页码。

示例输出内容包括：

```text
RAG回答
参考知识条数
参考来源
文件名
document_id
页码
```

---

## 4. 文档检索

进入 **“3. 文档检索”** 选项卡。

操作步骤：

```text
1. 在下拉框中选择当前文档
2. 点击“切换当前文档”
3. 输入搜索关键词
4. 点击“搜索”
```

示例关键词：

```text
RAG
LLM
Poirot
亲子沟通
自由
SFT
向量化
相似度检索
```

系统会返回相关内容，并展示：

* 当前检索文档
* document_id
* 来源文件
* 页码信息
* 相似度分数
* 内容摘要

---

## 5. 生成引用格式

进入 **“3. 文档检索”** 选项卡。

操作步骤：

```text
1. 在下拉框中选择当前文档
2. 输入搜索关键词
3. 点击“生成引用格式”
```

系统会生成可复制引用格式。

示例输出：

```text
1. 《test_word.docx》，未知页码。
相关度：0.1616
引用内容：RAG 是检索增强生成技术。它的核心流程包括：文档切分、向量化、相似度检索、基于上下文生成答案。
```

该功能可以用于整理学习笔记、学习报告和引用来源。

---

## 6. 添加学习笔记

进入 **“4. 学习笔记”** 选项卡。

填写概念名称和学习笔记。

示例：

```text
概念：RAG
笔记：RAG 的核心是先检索相关资料，再让大模型基于资料生成答案。
```

点击：

```text
保存笔记
```

学习笔记会保存到学习历史中，并可在 **“记忆回忆”** 和 **“学习报告”** 中查看。

---

## 7. 清空全部学习笔记

进入 **“4. 学习笔记”** 选项卡。

点击：

```text
清空全部学习笔记
```

系统会清空本地学习历史中的学习笔记，但不会删除：

```text
当前文档
RAG 知识库
历史问答记录
文档导入记录
```

---

## 8. 记忆回忆

进入 **“5. 记忆回忆”** 选项卡。

输入要回忆的关键词。

示例：

```text
RAG
亲子沟通
LLM
学习笔记
```

系统会同时查询：

```text
当前记忆系统
本地学习历史 JSON
历史问答记录
历史学习笔记
历史文档记录
```

然后返回相关历史内容。

---

## 9. 学习统计

进入 **“6. 学习统计”** 选项卡。

点击：

```text
查看统计
```

系统会显示当前学习助手的运行状态，包括：

* user_id
* session_id
* 当前文档
* 当前 document_id
* 已导入文档数
* 提问次数
* 学习笔记数
* Memory 系统状态
* RAG 知识库状态

---

## 10. 学习报告

进入 **“7. 学习报告”** 选项卡。

点击：

```text
学习生成报告
```

系统会生成完整学习报告，内容包括：

* 学习基本信息
* 最近导入文档
* 记忆系统状态
* RAG 知识库状态
* 最近问答记录
* 最近学习笔记
* 推荐复习方向

---

## 11. 导出 Markdown 报告

在 **“7. 学习报告”** 页面中点击：

```text
导出学习报告 Markdown
```

系统会生成 `.md` 格式的学习报告，并在页面中提供下载。

报告默认保存位置：

```text
D:\python_self_agent\reports
```

---

## 12. 导出 Word 报告

在 **“7. 学习报告”** 页面中点击：

```text
导出学习报告 Word
```

系统会生成 `.docx` 格式的 Word 学习报告，并在页面中提供下载。

Word 报告包含：

* 居中标题
* 生成时间
* 用户信息
* 分级标题
* 列表格式
* 正文段落
* 自动生成结尾

报告默认保存位置：

```text
D:\python_self_agent\reports
```

---

## 13. 删除当前文档

进入 **“2. 文档问答”** 或 **“3. 文档检索”** 页面。

操作步骤：

```text
1. 选择某个文档
2. 点击“切换当前文档”
3. 点击“删除当前文档”
```

系统会自动完成：

* 删除该文档对应的 RAG chunks
* 删除学习历史中的文档记录
* 删除该文档相关问答记录
* 重置当前文档状态
* 自动刷新文档下拉框

---

## 14. 清空全部文档

进入 **“2. 文档问答”** 或 **“3. 文档检索”** 页面。

点击：

```text
清空全部文档
```

系统会自动完成：

* 清空全部 RAG 知识库 chunks
* 删除所有历史文档记录
* 删除所有历史问答记录
* 重置当前文档状态
* 自动刷新文档下拉框

注意：默认会保留学习笔记。

---

## 15. 演示重点

本项目重点展示以下能力：

* Memory + RAG 结合
* 多格式文档解析
* PDF / TXT / Markdown / Word 文档导入
* 多文档管理
* 当前文档隔离问答
* 文档检索
* 页码级引用来源
* 检索结果按页去重
* 总结类问题全文抽样摘要
* 可复制引用格式生成
* 学习笔记保存
* 清空全部学习笔记
* 记忆回忆
* 学习历史持久化
* RAG JSON 本地持久化
* 学习报告自动生成
* Markdown 报告导出
* Word 报告导出

---

## 16. 最终项目检查清单

项目最终检查时，可以确认以下文件是否存在并可正常运行：

```text
README.md ✅
PROJECT_DEMO.md ✅
requirements.txt ✅
.env ✅
ui/gradio_app.py ✅
assistants/pdf_learning_assistant.py ✅
hello_agents/tools/builtin/rag_tool.py ✅
hello_agents/tools/builtin/memory_tool.py ✅
hello_agents/memory/rag/pipeline.py ✅
knowledge_base/rag_cache ✅
memory_data ✅
reports ✅
```

---

## 17. 完整测试流程

建议最终完整测试以下流程：

```text
1. 启动 Gradio 应用
2. 上传 PDF
3. 上传 TXT
4. 上传 Markdown
5. 上传 Word / DOCX
6. 切换当前文档
7. 文档问答
8. 文档检索
9. 生成引用格式
10. 添加学习笔记
11. 清空全部学习笔记
12. 记忆回忆
13. 查看学习统计
14. 生成学习报告
15. 导出 Markdown 报告
16. 导出 Word 报告
17. 删除当前文档
18. 清空全部文档
```

如果以上流程都能正常执行，说明项目已经具备完整的智能文档学习助手功能闭环。

---

## 18. 项目展示说明

本项目可以作为以下类型的展示项目：

```text
AI Agent 项目实践
RAG 检索增强生成项目
Memory + RAG 综合实验
智能文档问答助手
AI 学习助手
课程设计项目
毕业设计项目
简历项目
面试展示项目
```
